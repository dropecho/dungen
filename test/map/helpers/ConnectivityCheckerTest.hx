package map.helpers;

import massive.munit.Assert;
import degen.map.Map2d;
import degen.bsp.BspData;
import degen.map.helpers.ConnectivityChecker;

class ConnectivityCheckerTest {
  @Test
  public function connected_map_should_return_true() {
    Assert.isTrue(true);
  }

  @Test
  public function disconnected_map_should_return_false() {
    Assert.isTrue(true);
  }
}
